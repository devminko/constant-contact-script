{
  "name": "constant-contact-script",
  "version": "1.0.0",
  "description": "Script that uses Constant Contact API to download customer list and leads list and purges duplicates from the leads list",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Orpheus Inc.",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.8.2",
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.1"
  }
}
